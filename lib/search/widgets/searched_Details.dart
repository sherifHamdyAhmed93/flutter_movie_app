import 'package:flutter/material.dart';

class SearchedDetails extends StatelessWidget {
  const SearchedDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
